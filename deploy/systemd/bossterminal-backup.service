[Unit]
Description=BossTerminal SQLite backup
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
# Adjust to wherever you cloned the repo on the VPS.
WorkingDirectory=/opt/bossterminal
ExecStart=/usr/bin/docker compose -f docker-compose.yml -f docker-compose.prod.yml --profile backup run --rm backup
